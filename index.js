/**
 * @format
 */

import { AppRegistry } from 'react-native';
import Mastrcorreapp from './Mastrcorreapp';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => Mastrcorreapp);
